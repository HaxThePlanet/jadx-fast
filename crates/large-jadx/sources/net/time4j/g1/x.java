package net.time4j.g1;

import java.util.Locale;

/* compiled from: UnitPatternProvider.java */
/* loaded from: classes3.dex */
public interface x {
    String A(Locale locale, v vVar, n nVar);

    String B(Locale locale, boolean z, n nVar);

    String C(Locale locale, v vVar, n nVar);

    String a(Locale locale, v vVar, n nVar);

    String c(Locale locale, v vVar, n nVar);

    String e(Locale locale);

    String f(Locale locale, v vVar, n nVar);

    String g(Locale locale, boolean z, n nVar);

    String h(Locale locale, v vVar, n nVar);

    String i(Locale locale, boolean z, n nVar);

    String j(Locale locale, v vVar, n nVar);

    String l(Locale locale, v vVar, n nVar);

    String o(Locale locale, boolean z, n nVar);

    String p(Locale locale, v vVar, n nVar);

    String q(Locale locale, v vVar, int i);

    String r(Locale locale, boolean z, n nVar);

    String s(Locale locale, v vVar, n nVar);

    String t(Locale locale, boolean z, n nVar);

    String x(Locale locale, boolean z, n nVar);
}
